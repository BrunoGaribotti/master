// theme.js
import { createTheme } from "@mui/material/styles";

// Crea el tema personalizado
const theme = createTheme({
  typography: {
    fontFamily: "'Red Hat Display', sans-serif", // Configura la fuente global
  },
});

export default theme;